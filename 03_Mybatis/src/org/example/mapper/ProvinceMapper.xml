<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ProvinceMapper">
    <select id="selectProvince" resultType="Province">
        select * from province where id = #{id}
    </select>
    <select id="getAll" resultType="Province">
        select * from province
    </select>
    <insert id="insertProvince">
        insert into province(name, jiancheng, shenghui) values(#{name}, #{jiancheng}, #{shenghui})
    </insert>

</mapper>